# Readable Project


## How to start

* Install and start the API server
    - `cd api-server`
    - `npm install`
    - `node server`
* In another terminal window, start the frontend app
    - `cd frontend`
    - `npm install`
    - `npm start`
